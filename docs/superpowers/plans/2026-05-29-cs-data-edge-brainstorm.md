# CS Data Edge Brainstorm And Product Completion Plan

Date: 2026-05-29

## Purpose

This document captures what the HLTV scraper still needs to become a complete product, then expands the data strategy beyond basic maps, scores, K/D, and player stats. The goal is to build the richest possible point-in-time dataset for predicting future team encounters.

The guiding product question is:

> What did we know before the match, what happened during the match, and what repeatable patterns can we turn into predictive features without leaking future information?

## Product Completion Checklist

The scraper is already close to an operator-ready MVP. To feel like a complete product, it needs these remaining pieces.

### 1. Event Tier Registry

Current behavior uses HLTV result stars plus event-name overrides. A product-grade version should have a maintained registry:

```yaml
events:
  - pattern: "IEM Cologne"
    tier: 1
    tags: ["lan", "elite", "esl"]
  - pattern: "ESL Pro League"
    tier: 1
    tags: ["league", "elite", "esl"]
  - pattern: "CCT"
    tier: 2
    tags: ["online", "regional"]
```

Why it matters:

- HLTV stars are useful but not a full business definition.
- Some event families vary by year, region, qualifier stage, and team pool.
- Modeling needs stable tier labels to compare like-for-like matches.

### 2. Fixture Validator

Add a `validate-fixtures` command that checks every parsed JSON file for:

- Schema version.
- Required fields.
- Valid teams, event, scheduled time, and status.
- Valid map names and scores.
- Winner consistency with scores.
- Best-of consistency with map count.
- Player count sanity.
- Player stat completeness.
- Tier metadata presence.

Output should be JSON plus a nonzero exit code if the dataset is not training-safe.

### 3. Completeness And Progress Dashboard

CLI JSON is good for operations, but a dashboard/report makes the scraper feel like a product.

Minimum dashboard sections:

- Total discovered, parsed, final, open, due, failed.
- Tier 1 vs tier 2 fixture coverage.
- Matches with maps, vetoes, players, and player stats.
- Backfill next page, offset, started/completed state.
- Request success/failure rates by fetcher and proxy region.
- Latest fixtures and backups.
- Worst quality warnings.

This can start as a static HTML report generated by `python -m scraper.cli report`.

### 4. Better Stats Recovery

Stats pages are the main gap. The scraper should separate match-page completion from stats-page completion:

- Match complete without player stats.
- Stats URL missing.
- Stats URL found but blocked.
- Stats URL found but parser failed.
- Map stats ID found but map stats fetch failed.
- Map stats fetched but no player rows parsed.

Add commands:

- `retry-stats-only`
- `stats-gaps`
- `show-match MATCH_ID`

### 5. Date-Based Historical Backfill

The current cursor is page-based. Product language should be date-based:

```env
HLTV_BACKFILL_STOP_DATE=2023-09-27
```

This lets the target be:

- All CS2 tier 1 and tier 2 matches.
- All matches since CS2 release.
- All matches from a specific season.

Page-based limits can remain as a safety fallback.

### 6. Alerting And Daily Digest

Alerts should cover:

- Backfill finished.
- Failure rate above threshold.
- No new matches discovered in N runs.
- Stats coverage below target.
- Daily cap reached too early.
- Disk space low.
- Backup failed.

Daily digest payload:

- New discovered.
- New final fixtures.
- New failed rows.
- Quality score.
- Tier 1/tier 2 counts.
- Backfill cursor.
- Manifest snapshot ID.

### 7. Data Contract And Snapshot Manifest

Every dataset export should include:

- Fixture schema version.
- Parser version.
- Git SHA.
- Data snapshot hash.
- File count.
- Tier coverage.
- Quality score.
- Generation timestamp.

The manifest is the bridge between scraping and modeling. A model should always be trained against a named data snapshot, not a loose folder.

### 8. Source Audit Trail

Every fixture should be traceable back to raw inputs:

- Match page URL.
- Stats page URL.
- Map stats URLs.
- Raw HTML hashes.
- Fetch timestamps.
- Parser version.
- Fetcher type.
- Proxy region.

This is useful for debugging, reproducibility, and explaining model inputs.

### 9. Admin Operations

Add safe commands so you never need to manually edit SQLite:

- `show-match MATCH_ID`
- `failed`
- `retry-failed`
- `retry-stats-only`
- `reset-backfill`
- `pause-backfill`
- `resume-backfill`
- `validate-fixtures`
- `report`
- `export-with-manifest`

### 10. Downstream Ingestion

Once a scrape run is healthy, the pipeline should optionally:

- Reparse raw HTML.
- Validate fixtures.
- Write manifest.
- Export to Betto canonical fixture directory.
- Trigger Betto ingestion.
- Write an ingestion report.

## Data Extraction Brainstorm

The strongest predictive edge will probably come from combining many weak signals:

- Structural match context.
- Team strength and form.
- Map pool and veto tendencies.
- Roster and role fit.
- Player form.
- Event pressure and schedule fatigue.
- Tactical style.
- Economy, pistol, conversion, clutch, and opening-duel patterns.
- News, interview, and broadcast information.

HLTV alone gives a good base. Demos, VODs, transcripts, and LLM extraction can make the dataset much more unique.

## HLTV Data To Extract

### Match-Level Fields

From match pages and results pages:

- HLTV match ID.
- Scheduled time.
- Match status.
- Teams and team IDs.
- Event and event ID.
- Event tier.
- Stage or bracket round.
- Best-of format.
- LAN/online if visible or inferred from event.
- Prize pool and tournament importance if visible from event pages.
- Match page title and slug.
- Team rankings near match time if available.
- Head-to-head module if present.
- Betting odds if shown historically on match page.
- Stream links if present.
- VOD/demo links if present.

Predictive uses:

- BO1/BO3/BO5 changes variance.
- LAN vs online affects some teams heavily.
- Event tier affects effort, pressure, and opponent quality.
- Stage affects motivation and pressure.

### Event-Level Fields

From event pages:

- Event name.
- Organizer.
- Location.
- Country.
- LAN/online.
- Prize pool.
- Number of teams.
- Format.
- Stage structure.
- Dates.
- Participating teams.
- Final standings.

Predictive uses:

- Travel/time-zone effects.
- LAN pressure.
- Strength of field.
- Incentives and elimination pressure.
- Team adaptation across long events.

### Team-Level Fields

From team pages and rankings:

- Current roster.
- Coach.
- Country/region.
- Team ranking.
- Ranking trend.
- Recent matches.
- Recent events.
- Map win rates.
- Opponent quality faced.
- Roster age.
- Days since roster change.
- Stand-in indicators.

Predictive uses:

- Roster stability.
- Recent form adjusted for opponent quality.
- Regional style and travel effects.
- Team map pool depth.

### Player-Level Fields

From player pages and stats pages:

- Player ID.
- Nickname.
- Real name if needed.
- Country.
- Age.
- Team.
- Rating 2.0 / 3.0.
- ADR.
- KAST.
- Impact.
- KPR.
- DPR.
- Opening kill attempts.
- Opening kill success.
- Clutch stats.
- Headshot percentage.
- Map-specific performance.
- LAN vs online split.
- Last 30/60/90 day form.

Predictive uses:

- Player form.
- Role fit.
- Star-player dependency.
- Support-player consistency.
- LAN drop-off or LAN boost.

### Map-Level Fields

From match pages and map stats pages:

- Map name.
- Map index.
- Pick/decider status if inferable from veto.
- Team scores.
- Half scores.
- Overtime indicator.
- CT/T side starts if available.
- Winner.
- Player map stats.
- Rating by player.
- ADR by player.
- K-D by player.
- KAST by player.
- Opening kills/deaths.
- Clutches.

Predictive uses:

- Map-specific team strength.
- Side-specific weakness.
- Decider map performance.
- Ability to close maps.
- Star performance by map.

### Veto Fields

From match pages:

- Ban order.
- Pick order.
- Decider.
- Team responsible for each veto.
- Map removed/picked.

Derived features:

- First-ban frequency.
- First-pick frequency.
- Map exposure.
- Opponent-targeted bans.
- Whether a team avoids its statistical weak map.
- Whether a team lets opponent comfort maps through.
- Veto surprise score.
- Decider comfort.

Predictive uses:

- Expected map pool.
- Veto model.
- Map winner probability aggregation to match probability.

### Ranking And Form Fields

From HLTV rankings and team stats:

- Team world rank at match time.
- Rank delta over 1/2/4 weeks.
- Top-5/top-10/top-20 opponent record.
- Last N map win rate.
- Last N match win rate.
- Last N LAN win rate.
- Last N online win rate.
- Rest days.
- Maps played in last 7/14/30 days.
- Travel from previous event if event location is known.

Predictive uses:

- Schedule fatigue.
- Strength adjusted form.
- Rank momentum.

## Derived Features From Existing Scraped Data

Even without more sources, the current scraper can generate strong features:

### Team Form

- Team map win rate over 30/60/90/180 days.
- Match win rate over 30/60/90/180 days.
- Weighted recent form with exponential decay.
- Opponent-adjusted Elo/Glicko.
- Event-tier-adjusted Elo/Glicko.
- Map-specific Elo/Glicko.
- LAN-only and online-only ratings.

### Map Pool

- Map pick frequency.
- Map ban frequency.
- Map win rate when picked by team.
- Map win rate when picked by opponent.
- Decider win rate.
- Map pool entropy.
- Map comfort index.
- Veto predictability index.

### Player Form

- Player rating trend.
- ADR trend.
- KAST trend.
- Opening duel trend.
- Clutch conversion trend.
- Role-weighted form.
- Star-player volatility.
- Team dependency on top fragger.

### Roster Stability

- Days since last roster change.
- Core continuity.
- Player pair time together.
- Coach tenure.
- Stand-in count.
- New-player honeymoon effect.

### Context

- Rest days.
- Event stage.
- Elimination match flag.
- Grand final flag.
- Travel distance/time-zone delta.
- Same-event rematch flag.
- Back-to-back match flag.
- Number of maps played in the last 24/48/72 hours.

## Beyond HLTV: VOD, Stream Transcript, Demo, And LLM Data

HLTV gives structured results. The bigger edge may come from unstructured and semi-structured sources that markets price slowly or inconsistently.

### Stream Transcript Signals

If you transcribe broadcasts and feed them through an LLM, extract only timestamped, source-linked claims. The model should produce structured JSON with confidence and quote spans.

Useful transcript facts:

- Stand-in mentioned.
- Player illness.
- Visa/travel issues.
- Practice issues.
- Role changes.
- New IGL.
- Coach absence.
- Mental tilt.
- Team conflict rumors.
- Map preparation comments.
- Analyst desk veto expectations.
- Caster notes about unusual setups.
- Economy weakness.
- Pistol-round strength.
- Clutch reputation.
- Player confidence/body language comments.
- Recent scrim rumors, marked as low confidence.

Example output:

```json
{
  "match_id": "2390000",
  "timestamp": "00:14:32",
  "entity": "Team A",
  "signal": "stand_in",
  "value": true,
  "confidence": 0.92,
  "evidence": "Analyst says Team A are using a stand-in today.",
  "leakage_safe_before_match": true
}
```

Important rule:

- For pre-match predictions, only use transcript/VOD information published before the match starts.
- Post-match broadcast comments are useful for training tactical labels, but not as pre-match features for that same match.

### VOD Visual Signals

If you process VODs with OCR/computer vision:

- Pistol round outcomes.
- Force-buy outcomes.
- Eco conversion.
- Round economy state.
- Timeout usage.
- Tactical pause timing.
- Player camera/body language.
- Scoreboard snapshots.
- Utility damage if shown.
- Opening kill locations if minimap is visible.
- Bombsite hit tendency.
- Retake success.
- Save frequency.
- Clutch situations.

Predictive uses:

- Economy discipline.
- Tilt resilience.
- Tactical depth.
- Timeout effectiveness.
- Site preference by map.

### Demo-Derived Data

Demos are the best source for “best positions” and tactical truth. If demos are available, prioritize them over VOD inference.

Position and role features:

- Player average position by map and side.
- Anchor/lurk/entry/AWP/support role classification.
- Site anchor identity.
- Rotator identity.
- Default lane/path.
- Opening path heatmap.
- Retake position.
- Post-plant position.
- Utility throw locations.
- Flash assists.
- Trade proximity.
- Trade success.
- Spacing between teammates.
- Time-to-trade.
- Contact timing.
- Aggression score.
- Mid-round rotation timing.

Team tactical features:

- Default setup frequency.
- Site hit distribution.
- Execute timing.
- Fake frequency.
- Late-round success.
- Retake success.
- Save tendency.
- Utility efficiency.
- Flash effectiveness.
- Smoke/molly/HE damage.
- Crossfire quality.
- Map control timing.
- Opening duel selection.

Predictive uses:

- Role matchup: entry vs anchor weakness.
- Map-specific positional mismatches.
- Team exploitability.
- Tactical predictability.
- New roster role conflicts.

### Best Positions And Positional Strength

“Best positions” can mean several measurable things:

1. **Player position strength**
   - How well a player performs from common positions.
   - Example: Mirage connector, Inferno banana, Nuke ramp.

2. **Team positional control**
   - How often a team gains/holds key map areas.
   - Example: Ancient mid control success.

3. **Opponent weakness by position**
   - Which positions or map zones opponents fail to clear.

4. **Role comfort**
   - Whether a player is playing historically strong roles/positions.

Feature examples:

- `player.position_kill_rate_90d`
- `player.position_death_rate_90d`
- `player.position_trade_rate_90d`
- `team.zone_control_success_90d`
- `team.site_hold_success_90d`
- `team.retake_success_by_site_90d`
- `opponent.entry_success_vs_position_90d`
- `roster.role_disruption_score`

### LLM Tactical Labeling

An LLM can label qualitative match traits from transcripts, analyst desks, articles, and post-match interviews:

- “slow default team”
- “aggressive CT side”
- “weak late rounds”
- “AWPer in poor form”
- “new caller”
- “map pool expanded”
- “struggling on T side”
- “tilts after economy resets”
- “wins through individual skill”
- “structured execute-heavy team”

Convert these to structured features:

- Label.
- Entity.
- Time validity window.
- Confidence.
- Source URL/file.
- Evidence quote.
- Whether safe for pre-match use.

Avoid feeding free-text directly into betting decisions. Use structured labels and confidence.

## Additional External Data Sources

### Liquipedia

Potential fields:

- Event format.
- Stage and bracket details.
- Team rosters.
- Stand-ins.
- Transfers.
- Country/location.
- Prize pool.
- Match schedule.

### Steam/Demo Sources

Potential fields:

- Full round event logs.
- Kills/deaths with coordinates.
- Utility use.
- Economy.
- Damage.
- Bomb events.
- Player positions.

### News And Social

Potential fields:

- Roster rumors.
- Illness.
- Travel delays.
- Visa problems.
- Motivation.
- Bootcamp status.
- Practice comments.
- Public sentiment.

Need strict source quality scoring to avoid noisy rumor features.

### Odds And Market Data

Potential fields:

- Opening odds.
- Closing odds.
- Line movement.
- Implied probability.
- Book disagreement.
- Liquidity.
- Polymarket order-book depth.
- Sharp book vs public book difference.

Predictive uses:

- Market-implied strength.
- CLV evaluation.
- Public bias detection.
- Late information detection.

## Prediction Edge Feature Ideas

### Match Winner

- Team Elo/Glicko diff.
- Map pool expected value.
- Recent form diff.
- Roster stability diff.
- LAN/online split diff.
- Event-tier performance diff.
- Rest/fatigue diff.
- Market-implied probability.
- News/stand-in adjustment.

### Map Winner

- Map-specific Elo/Glicko diff.
- Pick/ban context.
- Side win-rate profile.
- Role/position matchup.
- Recent map exposure.
- Opponent map weakness.

### Correct Score

- Map pool depth.
- Upset variance.
- Decider strength.
- BO3 sweep tendency.
- Opponent adaptation across series.

### Total Maps

- Similar team strength.
- Map-pool overlap.
- Favorite’s weak map exposure.
- Underdog’s pick strength.
- Historical series length.

### Player Props If Available

- Player role and position.
- Map expected.
- Opponent positional weakness.
- Recent form.
- Team pace.
- Expected rounds.
- AWP usage.
- Entry involvement.

## Data Model Recommendation

Split data into layers:

1. **Raw**
   - HTML, VOD transcript, demo JSON, OCR output, source metadata.

2. **Parsed**
   - Matches, maps, vetoes, players, teams, stats, transcript claims.

3. **Validated**
   - Schema-checked, quality-scored, point-in-time safe records.

4. **Features**
   - Rolling and point-in-time feature values.

5. **Model Datasets**
   - Frozen snapshots with manifest IDs.

6. **Predictions And Evaluation**
   - Probabilities, recommendations, market prices, CLV, ROI.

## Prioritized Implementation Roadmap

### Now

1. Add `validate-fixtures`.
2. Add `show-match`.
3. Add `stats-gaps` and `retry-stats-only`.
4. Add date-based backfill stop.
5. Add static HTML report.

## Implementation Log

This section is updated as each roadmap item is completed.

- Complete: `validate-fixtures` CLI added. It validates schema version, required entities, scheduled time, best-of, maps, scores, tier metadata, player-count sanity, and player-stat count warnings.
- Complete: `show-match MATCH_ID` CLI added. It shows the queue row, parsed fixture summary, map/player-stat coverage, source URLs, and saved raw files for one match.
- Complete: `stats-gaps` and `retry-stats-only` CLIs added. They find finished fixtures whose maps are missing player stats and requeue those matches with `lifecycle_status = stats_gap` without editing SQLite manually.
- Complete: date-based backfill stop added with `HLTV_BACKFILL_STOP_DATE`. Discovery skips matches older than the stop date when scheduled dates are available, and auto-backfill marks itself done when the scanned page reaches the stop boundary.
- Complete: static HTML `report` CLI added. It writes queue, backfill, coverage, health checks, validation errors, and quality warnings into a browser-readable report.
- Complete: `tier-registry` CLI added with YAML-based tier registry (`tier_registry.yaml`). Events, tier overrides, and allow lists can be loaded from a YAML file via `HLTV_TIER_REGISTRY_PATH`. Env var overrides take precedence over registry file.
- Complete: stats fetch reliability improved. Added `stats_error` column to DB for categorized error tracking (404/timeout/blocked/rate_limited/server_error/parse_empty). Per-map stats retry with configurable max attempts. New `stats-errors` CLI shows error distribution.
- Complete: `scrape-events` CLI added. Fetches HLTV event pages and extracts location, country, LAN/online, prize pool, team count, format, dates, and participating teams. Stores raw HTML and parsed JSON.
- Complete: `scrape-teams` CLI added. Fetches HLTV team pages and extracts roster, coach, country, world ranking, and map win/loss stats. Stores raw HTML and parsed JSON.
- Complete: `scrape-players` CLI added. Fetches HLTV player pages and extracts country, age, team, rating, DPR, KAST, impact, ADR, KPR, HS%, and maps played. Stores raw HTML and parsed JSON.

### Scraper Completion (Pre-Collection Run)

All 10 data gaps identified in the pre-collection audit have been fixed.

1. **Upcoming match discovery** — Complete. Added `discover_upcoming()` and `discover-upcoming` CLI. Scrapes `hltv.org/matches` for scheduled and live matches with `is_live` flag.
2. **Half scores** — Complete. Added `team_a_first_half`, `team_b_first_half`, `team_a_second_half`, `team_b_second_half` fields to `ScrapedMap`. Parser extracts "9:6 / 4:7" patterns from map score text.
3. **Overtime detection** — Complete. Added `overtime: bool` field to `ScrapedMap`. Detected when total rounds > 30.
4. **CT/T player stats** — Complete. Enhanced `parse_map_stats_page()` to extract side-specific tables and merge CT/T kills/deaths per player. Added `clutches_won` extraction via labeled-int search. `_player_stat()` now fills all side fields.
5. **HLTV rankings scraper** — Complete. New `scraper/rankings.py` module with `scrape_rankings()`, `scrape_rankings_range()`, `parse_ranking_page()`. Stores weekly ranking snapshots as dated JSON. New `scrape-rankings` CLI with `--date`, `--start`, `--end` options.
6. **Match stage/round** — Complete. Added `_match_stage()` to parser extracting "Grand Final", "Semifinal", "Group Stage", etc. Added `match_stage` field to `ScrapedMatch` and fixture JSON.
7. **Head-to-head module** — Complete. Added `_head_to_head()` to parser extracting H2H win records from match pages. Added `head_to_head` field to `ScrapedMatch` and fixture JSON.
8. **Stars from match pages** — Complete. Fixed `_event()` to call `_match_page_stars()` which extracts stars from `[stars]` attribute, filled star CSS classes, or text patterns.
9. **Team recent results** — Complete. Added `_team_page_recent_results()` extracting recent match results with match_id, score, opponent, and event. Added `recent_results` field to team page output.
10. **Player per-map stats and recent form** — Complete. Added `_player_page_map_stats()` for map-specific win/loss stats and `_player_page_recent_form()` for recent match form with map_stats_id, map name, rating, kills/deaths. Added `per_map_stats` and `recent_form` fields to player page output.
11. **Rankings backfill** — Complete. Added `backfill-rankings` CLI that fetches all missing Monday ranking snapshots from CS2 release date (2023-09-27) or `HLTV_BACKFILL_STOP_DATE` to today. Added `rankings-status` CLI showing coverage percentage and missing dates. Added `rankings_status()` and `CS2_RELEASE_DATE` constant to rankings module.

### Next

All items complete.

1. Extract event pages for LAN/location/prize/format.
2. Extract team pages for roster and ranking.
3. Extract player pages for rolling stats.
4. Improve stats page fetch reliability.
5. Build tier registry file.

### Later

1. Add demo parser pipeline.
2. Build position/role heatmap features.
3. Build VOD transcript ingestion.
4. Add LLM structured claim extraction.
5. Add news/social source scoring.

## Backtesting And Prediction System

### Purpose

Before placing real bets, the system needs to prove its edge on historical data. The backtesting system takes every scraped match, computes features using only data available before that match, generates a prediction, and compares against the actual outcome. This is how we know whether we have edge and how large it is.

The core loop is:

```
for each match in chronological order:
    features = build_features(match, all_prior_data)
    prediction = model.predict(features)
    actual = match.outcome
    record(prediction, actual)
evaluate(all_records)
```

### Architecture

Three modules that build on the scraper output:

```
scraper/features/       Feature engineering from fixture JSON + rankings
scraper/model/          Prediction models (Elo, Glicko-2, ML)
scraper/backtest/       Walk-forward simulation and evaluation
```

### Module 1: Feature Builder (`scraper/features/`)

The feature builder takes a match and all prior data, and returns a feature vector. Every feature must be point-in-time safe: it uses only information that was available before the match started.

#### Core Feature Categories

**Team Strength Ratings**

- Overall Elo rating per team, updated after each match result.
- Glicko-2 rating per team with rating deviation (uncertainty).
- Map-specific Elo: separate rating per team per map.
- LAN-only and online-only ratings.
- Elo/Glicko difference between team A and team B at match time.

Elo update rule: after each map, update both teams' ratings using the map outcome. K-factor can vary by event tier. Glicko-2 adds rating deviation that shrinks with more games and grows with inactivity.

**Rankings**

- Team A world ranking at match time (from weekly ranking snapshots).
- Team B world ranking at match time.
- Ranking difference.
- Ranking trend: delta over 1, 2, and 4 weeks.
- Whether team is top 5, top 10, or top 20.

Implementation: for a given match date, find the most recent Monday ranking snapshot that predates the match. Look up both teams in that snapshot.

**Recent Form**

- Match win rate over last 30, 60, 90 days.
- Map win rate over last 30, 60, 90 days.
- Weighted recent form with exponential decay (more recent maps count more).
- Win rate against top-10, top-20, and below-20 opponents.
- Win rate in BO1, BO3, BO5 separately.
- Form on LAN vs online.

Implementation: scan all prior fixtures, filter by team, filter by date window, compute rates. For opponent-quality-adjusted form, cross-reference opponent ranking at the time of each historical match.

**Map Pool**

- Map pick frequency per team (how often each map appears when team plays).
- Map ban frequency per team (inferred from vetoes).
- Map win rate per team per map.
- Map win rate when picked by the team vs picked by opponent.
- Decider map win rate.
- Map pool depth: number of maps with >50% win rate over last 90 days.
- Map pool entropy: how evenly spread the team's maps are.
- Map comfort index per map: combination of pick frequency and win rate.

**Veto Prediction Features**

- First ban frequency per map per team.
- First pick frequency per map per team.
- Opponent-targeted ban rate (does team A ban team B's best map?).
- Veto surprise score: how often does a team deviate from its historical pattern.
- Expected map pool overlap given both teams' histories.

**Player Form**

- Average player rating over last 10, 20, 30 maps.
- ADR trend over recent maps.
- KAST trend.
- Opening kill rate trend.
- Clutch conversion rate.
- Star player dependency: fraction of team's total kills from the top fragger.
- Support player consistency: variance of non-star player ratings.
- Per-map player rating (player X on Inferno vs player X on Mirage).

**Roster Stability**

- Days since last roster change (requires tracking roster snapshots from team pages).
- Number of roster changes in last 90 days.
- Core continuity: number of players who have been on the team for >6 months.
- Stand-in indicator: if any player in the lineup does not match the stored roster.
- Coach tenure in days.

**Match Context**

- Best-of format (BO1, BO3, BO5).
- LAN or online.
- Event tier (1 through 5).
- Match stage (group, quarterfinal, semifinal, grand final).
- Elimination match flag (does the loser go home?).
- Rest days since team's last match.
- Maps played in last 24, 48, and 72 hours (fatigue proxy).
- Head-to-head record between the two teams over last 6 and 12 months.
- Same-event rematch flag (have they already played each other in this event?).

**Half And Side Features**

- Team CT-side win rate over recent maps.
- Team T-side win rate over recent maps.
- CT-side vs T-side differential (is the team CT-heavy or T-heavy?).
- First half win rate.
- Comeback rate (how often does the team win after losing the first half?).
- Overtime rate (how often do their maps go to overtime?).

#### Feature Vector Format

Each match produces one feature row:

```python
@dataclass
class MatchFeatures:
    match_id: str
    scheduled_at: datetime
    team_a_id: str
    team_b_id: str

    # Ratings
    elo_diff: float
    glicko_diff: float
    glicko_rd_a: float
    glicko_rd_b: float
    ranking_a: int | None
    ranking_b: int | None
    ranking_diff: int | None

    # Form
    form_30d_a: float | None
    form_30d_b: float | None
    form_60d_a: float | None
    form_60d_b: float | None
    map_winrate_30d_a: float | None
    map_winrate_30d_b: float | None

    # Map pool
    map_pool_depth_a: int
    map_pool_depth_b: int
    map_pool_overlap: int

    # Context
    best_of: int
    is_lan: bool | None
    event_tier: int | None
    match_stage: str | None
    is_elimination: bool
    rest_days_a: float | None
    rest_days_b: float | None
    h2h_wins_a: int
    h2h_wins_b: int

    # Player
    star_rating_a: float | None
    star_rating_b: float | None
    roster_days_a: int | None
    roster_days_b: int | None
```

The feature builder outputs these rows into a CSV or Parquet file indexed by match_id. The model consumes this file.

#### Point-In-Time Safety

The most important rule: no feature can use information from after the match started. This means:

- Form windows must end before `scheduled_at`.
- Rankings must use the most recent snapshot before `scheduled_at`.
- Elo ratings must be computed from matches that finished before `scheduled_at`.
- Player stats must use only maps played before `scheduled_at`.

The feature builder must enforce this strictly. A single leak (using the match's own result or a future match's result to compute a feature) invalidates the entire backtest.

Implementation: sort all matches by `scheduled_at`. Process them in order. After computing features for match N, update the internal state (Elo, form arrays, etc.) using match N's result, so that match N+1 can use the updated state. This is the walk-forward approach.

### Module 2: Prediction Models (`scraper/model/`)

Start with simple models and layer complexity only when the simple model's weaknesses are understood.

#### Model 1: Elo Baseline

The simplest useful model. Each team has a single Elo rating. After each match, update ratings based on the outcome. The prediction for a new match is the win probability derived from the Elo difference.

```
P(team_a_wins) = 1 / (1 + 10^((elo_b - elo_a) / 400))
```

Configuration:
- Starting Elo: 1500 for all teams.
- K-factor: 32 for tier 1 events, 24 for tier 2, 16 for tier 3+.
- Update per map or per match (per map is more granular).
- Optional: home/away advantage for LAN.

Expected accuracy: 58-63% on match winner prediction.

This model exists only to set the floor. If a complex model cannot beat Elo, it has no value.

#### Model 2: Glicko-2

Same concept as Elo but tracks rating deviation (uncertainty). A team that has not played in 30 days has higher uncertainty, which means less confident predictions. This naturally handles teams returning from breaks.

Advantages over Elo:
- Confidence intervals on predictions.
- Inactive teams' ratings widen automatically.
- Can be extended per-map with separate Glicko-2 systems.

#### Model 3: Map-Level Glicko-2

Maintain separate Glicko-2 ratings per team per map. To predict a BO3 match:

1. Predict veto using historical pick/ban frequencies.
2. For each likely map, predict map winner using map-specific Glicko-2.
3. Aggregate map win probabilities into match win probability.

This is where the edge starts to show. Markets often price matches at the match level, but map-level strength varies significantly. A team might be 60% to win overall but 80% on their pick and 35% on the opponent's pick.

#### Model 4: Feature-Based ML

Logistic regression or gradient boosted trees (XGBoost/LightGBM) using the full feature vector from Module 1. This can capture interactions that rating systems miss:

- LAN vs online effects for specific teams.
- Fatigue effects.
- Event-tier-specific performance.
- Roster stability interactions.

Training set: all matches before a cutoff date.
Validation set: matches after the cutoff.
Feature selection: start with top 10 features by importance, add more only if validation improves.

Overfitting risk: with ~2000-3000 CS2 matches and 30+ features, regularization matters. Use L1/L2 regularization, limit tree depth, and validate on a held-out time period (not random split — always time-split to avoid leakage).

#### Model Outputs

Every model produces:

```python
@dataclass
class Prediction:
    match_id: str
    model_name: str
    model_version: str
    team_a_win_prob: float      # 0.0 to 1.0
    team_b_win_prob: float      # 1.0 - team_a_win_prob
    confidence: float           # model-specific confidence (e.g., 1/RD for Glicko)
    features_used: dict         # snapshot of input features for reproducibility
    predicted_at: datetime
```

### Module 3: Walk-Forward Backtester (`scraper/backtest/`)

The backtester iterates through all historical matches and simulates what would have happened if we had bet using the model's predictions.

#### Backtest Loop

```python
def run_backtest(matches, model, bet_strategy):
    state = ModelState()       # Elo ratings, form arrays, etc.
    records = []

    for match in sorted(matches, key=lambda m: m.scheduled_at):
        features = build_features(match, state)
        prediction = model.predict(features)

        # Record prediction vs actual
        actual_winner = match.actual_winner()
        records.append(BacktestRecord(
            match_id=match.hltv_id,
            scheduled_at=match.scheduled_at,
            predicted_prob_a=prediction.team_a_win_prob,
            actual_winner=actual_winner,
            bet_decision=bet_strategy.decide(prediction, market_odds=None),
        ))

        # Update state with this match's actual result
        state.update(match)

    return BacktestResult(records)
```

#### Evaluation Metrics

**Accuracy metrics:**

- Raw accuracy: fraction of matches where the predicted winner was correct.
- Accuracy by confidence bucket: accuracy for predictions in [50-55%], [55-60%], [60-65%], etc.
- Accuracy by event tier, match format (BO1/BO3), LAN/online.

**Calibration:**

- Calibration curve: of matches where we predicted 60% team A, did team A actually win ~60% of the time?
- Perfect calibration means our probabilities are honest. If we predict 70% and team A wins 85% of the time at that level, the model is underconfident.
- Brier score: mean squared difference between predicted probability and outcome (0 or 1). Lower is better.
- Log loss: penalizes confident wrong predictions heavily. The primary metric for probability quality.

**Profitability simulation:**

- Flat bet ROI: if we bet $1 on every match where our edge exceeds a threshold, what is the return?
- Kelly criterion sizing: optimal bet size based on edge and bankroll. Simulate bankroll trajectory over time.
- CLV (Closing Line Value): compare our prediction at time of bet vs the closing line. Consistently beating the close is the strongest signal of real edge.
- Drawdown: maximum peak-to-trough bankroll decline. Critical for sizing.
- Sharpe ratio: risk-adjusted return.
- Win rate by edge bucket: of bets where our estimated edge was 5-10%, what was the actual ROI?

**Model comparison:**

- Compare Elo baseline vs Glicko-2 vs map-level Glicko-2 vs ML model.
- For each model, report accuracy, log loss, calibration, and simulated ROI.
- A model is only useful if it beats the Elo baseline by a meaningful margin on the validation period.

#### Bet Strategy Simulation

The backtester supports pluggable bet strategies:

**Threshold strategy:** Bet on team A if `predicted_prob_a > threshold` and the edge vs implied market odds exceeds a minimum.

**Kelly strategy:** Bet size = `(edge * odds - 1) / (odds - 1)`, capped at a fraction of bankroll. Requires market odds data.

**Flat strategy:** Equal bet size on every qualifying match. Simplest to analyze.

For initial backtesting without market odds data: use the model's own probabilities as a proxy. If the model says 65% and the Elo baseline says 55%, the "edge" is the difference. This is not the same as beating the market, but it shows whether the complex model adds value over the simple one.

Later, when Polymarket odds data is available: compare model probability against market-implied probability. Only bet when model probability exceeds market-implied probability by a configurable threshold (e.g., 5%).

#### Output Format

The backtester produces:

```
backtest_results/
  YYYY-MM-DD_model-name/
    summary.json          Overall metrics
    predictions.csv       Per-match predictions and outcomes
    calibration.json      Calibration curve data
    bankroll.csv          Simulated bankroll trajectory
    by_tier.json          Metrics broken down by event tier
    by_format.json        Metrics broken down by BO1/BO3
    by_month.json         Monthly accuracy and ROI
```

### Warm-Up Period

Rating systems need history to stabilize. The first N matches (where N is roughly 200-500) produce unreliable ratings because all teams start at the same Elo. The backtest should:

1. Use the first 6 months of data as the warm-up period (ratings stabilize but predictions are not evaluated).
2. Begin recording predictions only after the warm-up.
3. Report the warm-up cutoff date in the output.

### Data Requirements

The backtesting system consumes:

- **Fixture JSON files**: match results, maps, scores, players, vetoes, event tier.
- **Ranking snapshots**: weekly team rankings from `raw/rankings/YYYY-MM-DD.json`.
- **Event metadata**: LAN/online, location from `raw/events/{id}.json`.
- **Team metadata**: roster from `raw/teams/{id}.json` (for roster stability features).
- **Player metadata**: stats from `raw/players/{id}.json` (for player form features).

All of this is already being scraped. The feature builder reads these files directly.

### Implementation Order

**Phase 1: Elo Baseline (simplest useful model)**

1. Build Elo rating engine that processes fixtures chronologically.
2. Build feature builder that outputs match_id, elo_a, elo_b, elo_diff, actual_winner.
3. Build backtester that iterates matches and records predictions.
4. Build evaluation module: accuracy, calibration, log loss.
5. Output: "Elo predicts CS2 match winners with X% accuracy."

**Phase 2: Glicko-2 + Map-Level Ratings**

1. Replace Elo with Glicko-2 (adds uncertainty tracking).
2. Add map-level Glicko-2 ratings.
3. Add veto prediction using historical pick/ban frequencies.
4. Build BO3 match probability from map-level predictions.
5. Compare: does map-level Glicko-2 beat overall Glicko-2?

**Phase 3: Feature Engineering**

1. Add ranking features (from ranking snapshots).
2. Add recent form features (30/60/90 day windows).
3. Add context features (LAN, tier, stage, rest days).
4. Add half-score features (CT/T side strength).
5. Add roster stability features.
6. Add player form features.

**Phase 4: ML Model**

1. Build training pipeline: features CSV → logistic regression → evaluation.
2. Feature importance analysis: which features actually help?
3. Try gradient boosting (XGBoost/LightGBM).
4. Time-split cross-validation.
5. Compare ML model vs Glicko-2 baseline.

**Phase 5: Bet Simulation**

1. Add bet strategy simulation with flat bet sizing.
2. Add Kelly criterion sizing.
3. Add bankroll trajectory tracking.
4. Add edge threshold analysis (what minimum edge is profitable?).
5. When Polymarket data is available: simulate against real market odds.

### CLI Commands

```
python -m scraper.cli build-features        Build feature CSV from fixtures
python -m scraper.cli backtest              Run walk-forward backtest
python -m scraper.cli backtest-report       Generate HTML report from backtest results
python -m scraper.cli model-compare         Compare multiple models side by side
python -m scraper.cli rankings-status       Show rankings coverage
python -m scraper.cli backfill-rankings     Scrape all missing ranking snapshots
```

### Success Criteria

The backtesting system is useful if:

1. Elo baseline shows >58% accuracy on CS2 matches (sanity check that the data is good).
2. Map-level Glicko-2 beats overall Glicko-2 by >1% accuracy (confirms map-level modeling has value).
3. The ML model beats Glicko-2 by >1% on log loss (confirms feature engineering adds value beyond ratings).
4. Calibration curve shows the model is well-calibrated within 3% across all probability buckets.
5. Simulated ROI against model-implied fair odds is positive at >2% edge threshold.
6. No feature leaks future data (verified by the walk-forward structure).

If the Elo baseline cannot reach 55% accuracy, the data quality needs investigation before adding complexity.

## Biggest Potential Edge Sources

The likely highest edge features are:

1. **Roster disruption**
   - Stand-ins, role swaps, new IGL, short practice time.

2. **Map veto prediction**
   - Getting expected maps right before market fully prices them.

3. **Map-specific team strength**
   - Better than global team rating.

4. **Role and position matchup**
   - Especially from demos: weak anchor vs strong execute team, entry path mismatch, exploitable site.

5. **Fatigue and schedule density**
   - Many markets underweight travel, back-to-back matches, and long event days.

6. **Broadcast/news qualitative signals**
   - Illness, confidence, preparation, tactical changes, role conflict.

7. **Market movement**
   - Combining model probability with line movement and liquidity can separate real edge from stale numbers.

## Guardrails

- Never use information published after match start for pre-match predictions.
- Every feature needs an `as_of` timestamp.
- LLM-extracted claims need source, timestamp, confidence, and evidence.
- Rumor/social features should be low confidence unless confirmed.
- Raw data should always be stored before parsing.
- Every model run should cite a manifest snapshot ID.
